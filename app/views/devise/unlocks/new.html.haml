.container.d-flex.justify-content-center.align-items-center.min-vh-100
  .card.shadow-lg{style: "width: 40rem; border-radius: 15px;"}
    .card-body.p-5
      %h2.text-center.mb-4 Resend unlock instructions

      = simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f|
        = render "devise/shared/error_messages", resource: resource

        .field.mb-4
          = f.input :email, label: "Email", input_html: { class: 'form-control form-control-lg' }, autofocus: true, autocomplete: "email"

        .actions.text-center
          = f.submit "Resend unlock instructions", class: 'btn btn-primary w-100 btn-lg'

      .text-center.mt-5
        = render "devise/shared/links"
